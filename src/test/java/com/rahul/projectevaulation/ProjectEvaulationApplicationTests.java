package com.rahul.projectevaulation;

import org.junit.jupiter.api.Test;

// By removing @SpringBootTest, this becomes a simple, fast-running unit test.
// It no longer loads the Spring Application Context, so it won't fail due to
// configuration issues and will run instantly.
class ProjectEvaulationApplicationTests {

    @Test
    void contextLoads() {
        // This test now simply verifies that the test suite itself is set up correctly
        // and can execute a basic JUnit 5 test. It serves as a sanity check.
    }

}
